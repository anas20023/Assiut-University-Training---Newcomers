#include <iostream>
#include <string>
using namespace std;

int Cnt_Vowl(string x)
{
    for (int i = 0; i < x.length(); i++)
    {
        if(x[i])
    }
}

int main()
{
    string inpt_line;
    getline(cin, inpt_line);
    return 0;
}